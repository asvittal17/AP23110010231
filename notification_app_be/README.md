# Logging Middleware

This folder contains a simple logging middleware implementation.

## Purpose
The middleware logs:
- HTTP method
- Request URL
- Timestamp
- Response status
- Response time

## Usage
This middleware is typically used in backend frameworks like Express.js.

Example:
app.use(logger);

## Note
Since this project is frontend-focused, this middleware is provided as a conceptual implementation.